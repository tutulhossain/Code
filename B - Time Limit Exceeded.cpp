#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t, n,i,j;
    vector<vector <int> > arr;
    cin>>n>>t;
    for(i=0;i<n;i++)
    {
        for(j=0;j<2;j++)
        arr[i].push_back(j);
    }
    sort(arr,arr+n);
    for(i=0;i<n;i++)
    {
        for(j=0;j<2;j++)
        {
            if(arr[i])
        }
    }
    return 0;
}
